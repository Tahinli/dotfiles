# Monitor names, sourced by the niri scripts. Not executable on purpose.
#
# Swapping a monitor means editing this file plus the `output` blocks and the
# four focus-monitor / move-column-to-monitor binds in config.kdl. KDL has no
# variables, so those cannot be parameterised — config.kdl names the outputs
# in exactly one place each, and this file covers everything scripted.

LEFT_MONITOR="DP-1"      # Xiaomi Mi Monitor, 2560x1440@180, primary, at x=0
RIGHT_MONITOR="HDMI-A-1" # 1920x1080@75, to the right at x=2560
