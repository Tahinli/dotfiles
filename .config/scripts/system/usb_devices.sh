# Device map for usb_menu.sh — sysfs paths under /sys/bus/usb/devices.
# `usb_menu.sh list` prints the current paths in parentheses.
# Paths change only if you move the device to another physical port/hub.

# Keyboard (ASUS ROG OMNI RECEIVER — also carries other ROG wireless gear).
KEYBOARD_USB="1-5.4"
