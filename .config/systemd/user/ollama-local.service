[Unit]
Description=ollama (upstream build, user-local)
After=graphical-session.target

[Service]
Type=simple
Environment=OLLAMA_MODELS=%h/.ollama/models
ExecStart=%h/.local/share/ollama-up/dist/bin/ollama serve
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
